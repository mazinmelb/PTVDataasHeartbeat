{ app.beginUndoGroup("caption timing");  // Grab  current composition   var currentComp     = app.project.activeItem;  var startTime = 0; // start at 0 seconds  var totalTime = 0; // start at 0 seconds  var viewableSecs = 5;    var compSettings    = cs = [1280, 720, 1, 10, 24];       // Creating background layer   var backgroundLayer = currentComp.layers.addSolid([93, 5, 2], "Background", cs[0], cs[1], cs[2]);// wowrk backwards from 2 minutes 30   var endTime = 160;                     // loop through eah layers                for (var i = 1; i<=currentComp.numLayers; i++)                {                     var thisCaption = currentComp.layers[i];                                           thisCaption.outPoint = (endTime);                    endTime = endTime- viewableSecs;                    thisCaption.inPoint = endTime                    // put timing marker                       backgroundLayer.property("Opacity").setValueAtTime(endTime,100);                     }  app.endUndoGroup();}